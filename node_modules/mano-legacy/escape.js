'use strict';

var $ = require('./main')

  , el = document.createElement('div')
  , node = el.appendChild(document.createTextNode(''));

module.exports = $.escape = function (txt) {
	node.data = txt;
	return el.innerHTML.replace('"', '&quot;');
};
