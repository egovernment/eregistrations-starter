'use strict';

var $ = require('./main');

module.exports = $.elementInElementInclusive = function (child, parent) {
	if (!child || !parent) throw new TypeError("Invalid elements");

	while (child) {
		if (child === parent) return true;
		child = child.parentNode;
	}
	return false;
};
