'use strict';

var $      = require('./main')
  , extend = require('./element#/extend')

  , slice = Array.prototype.slice;

module.exports = $.makeElement = function (name, attrs) {
	var el = $(document.createElement(name));

	return extend.apply(el, slice.call(arguments, 1));
};
