'use strict';

var count = 0;

module.exports = function (el) {
	if (!el || !el.nodeName) throw new TypeError(el + " is not a DOM node");
	if (el.domLegacy$id) return el.domLegacy$id;
	return (el.domLegacy$id = ++count);
};
