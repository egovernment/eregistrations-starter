'use strict';

var $ = require('./main');

module.exports = $.checkAll = function (container) {
	var inputs = $(container).getElementsByTagName('input'), i, input;
	for (i = 0; (input = inputs[i]); ++i) {
		if (input.type !== 'checkbox') continue;
		input.checked = true;
	}
};
