'use strict';

var $ = require('./main');

module.exports = $.fillSelects = function (container, value) {
	var selects = $(container).getElementsByTagName('select'), i, element;
	for (i = 0; (element = selects[i]); ++i) {
		element.value = value;
	}
};
