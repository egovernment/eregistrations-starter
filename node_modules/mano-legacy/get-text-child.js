'use strict';

var $ = require('./main');

module.exports = $.getTextChild = function (el) {
	var node;
	el = $(el);
	node = $(el).firstChild;
	if (node && (node.nodeType === 3)) return node;
	return el.insertBefore(el.ownerDocument.createTextNode(""), el.firstChild);
};
