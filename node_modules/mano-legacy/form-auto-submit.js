'use strict';

var $ = require('./main')

  , dispatchEvent = require('./dispatch-event');

require('./element#/event');

module.exports = $.formAutoSubmit = function (id) {
	var form = $(id);
	form.addEvent('change', function () {
		setTimeout(function () { dispatchEvent(form, 'submit'); }, 0);
	});
};
