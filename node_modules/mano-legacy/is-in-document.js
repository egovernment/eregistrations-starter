'use strict';

var $ = require('./main');

module.exports = $.isInDocument = function (el) {
	el = el.parentNode;
	while ((el = el.parentNode)) {
		if (el === document) return true;
	}
	return false;
};
