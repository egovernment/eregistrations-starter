'use strict';

var $ = require('./main');

module.exports = $.elementInDocument = function (el) {
	if (el.ownerDocument !== document) return false;
	while ((el = el.parentNode)) if (el === document) return true;
	return false;
};
