'use strict';

var hasOwnProperty = Object.prototype.hasOwnProperty;

module.exports = function (obj, key, submitData) {
	var id = obj.__id__ + '/' + key;
	if (hasOwnProperty.call(submitData, id)) return submitData[id];
	return obj[key];
};
