#!/usr/bin/env node

// Generates index.html for this app
// This module was initially generated by mano/scripts/generate-app-html-index-script

'use strict';

Error.stackTraceLimit = Infinity;

var resolve  = require('path').resolve
  , generate = require('mano/scripts/generate-app-html-index');

generate(
	resolve(__dirname, '../index.html.tpl'),
	resolve(__dirname, '../', require('../mano').viewPath, 'index.html'),
	require('${ rootDir }/../env')
).done();
