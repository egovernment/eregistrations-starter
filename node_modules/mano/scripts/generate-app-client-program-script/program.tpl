#!/usr/bin/env node

// Generates client programs for this app
// This module was initially generated by mano/scripts/generate-app-client-program-script

'use strict';

Error.stackTraceLimit = Infinity;

var resolve  = require('path').resolve
  , generate = require('mano/scripts/generate-app-client-program');

generate(resolve(__dirname, '..'), {
	minify: (require('${ rootDir }/../env').minify !== false),
	viewPath: require('../mano').viewPath
}).done();
