// DO NOT EDIT. FILE WAS AUTOGENERATED

'use strict';

var db  = module.exports = require('mano').db
  , d   = require('d')
  , Set = require('dbjs/plain-set')
	, unserializeKey = require('dbjs/_setup/unserialize/key')

  , get = db.objects.unserialize
  , defineProperties = Object.defineProperties
  , defineProperty = Object.defineProperty, now = Date.now
  , desc = { configurable: true, enumerable: true, writable: false }
  , setDesc = { configurable: true, enumerable: false, writable: false }
  , uKey = function (sKey) { return unserializeKey(sKey, db.objects); }

  , time, set, obj, item, toSet;

toSet = function (arr) {
	var set = new Set();
	setDesc.value = arr;
	defineProperty(set, '__setData__', setDesc);
	setDesc.value = null;
	return set;
};

//$LOG$time = now();
db._postponed_ += 1;

$IMPORT$

db._postponed_ -= 1;
desc.value = set = obj = null;

//$LOG$console.log("Meta restore: $COUNT$ properties in " +
//$LOG$	((now() - time) / 1000).toFixed(3) + "s");
