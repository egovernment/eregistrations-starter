// Generates fast client model version for provided apps

'use strict';

var runPrograms = require('mano/server/utils/run-programs')
  , resolve     = require('path').resolve;

module.exports = function (root, appsList) {
	return runPrograms(appsList.map(function (appPath) {
		return ['node', resolve(root, appPath, 'bin/generate-client-model')];
	}), 'setup', 'generate-apps-client-model', { cwd: root });
};
