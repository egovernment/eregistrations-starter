'use strict';

var memoize  = require('memoizee/plain')
  , path     = require('path')
  , readFile = require('fs2/read-file')

  , resolve = path.resolve;

module.exports = memoize(function (path) { return readFile(resolve(path, 'index.html')); });
