// Map of current business processes and user ids in form:
// <userId>: <businessProcessId>

'use strict';

var getMap = require('eregistrations/server/utils/get-db-map');

module.exports = getMap(require('mano').dbDriver.getStorage('user'), 'currentBusinessProcess');
