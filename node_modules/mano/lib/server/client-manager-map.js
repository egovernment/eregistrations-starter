// Map of manager clients and managers in form:
// <clientId>: <managerId>

'use strict';

var getMap = require('eregistrations/server/utils/get-db-map');

module.exports = getMap(require('mano').dbDriver.getStorage('user'), 'manager');
