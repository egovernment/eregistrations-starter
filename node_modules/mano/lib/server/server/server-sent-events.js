'use strict';

var connection = require('../sse-driver');

module.exports = function (req, res, next) {
	if (req._parsedUrl.pathname !== '/update-stream/') {
		next();
		return;
	}

	connection(req, res);
};
