'use strict';

var assign = require('es5-ext/object/assign')
  , st     = require('st');

module.exports = function (path, env/*, options*/) {
	return st(assign({ path: path, passthrough: true, index: false, cache: !env.dev },
		Object(arguments[2])));
};
