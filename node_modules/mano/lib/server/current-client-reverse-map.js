// Map of currently managed user and manager ids in form:
// <clientId>: <managerId>

'use strict';

var getReverseMap = require('eregistrations/server/utils/get-db-reverse-map');

module.exports = getReverseMap(require('mano').dbDriver.getStorage('user'), 'currentlyManagedUser');
