// Map of currently managed user and manager ids in form:
// <managerId>: <clientId>

'use strict';

var getMap = require('eregistrations/server/utils/get-db-map');

module.exports = getMap(require('mano').dbDriver.getStorage('user'), 'currentlyManagedUser');
