// Session id, thinking browser session (each tab/window has distinct id)
// not to be mistaken with server side authentication id)

'use strict';

module.exports = sessionStorage.manoSessionId = require('time-uuid')();
