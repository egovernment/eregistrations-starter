// Initialize DomJS engine, it's just template engine we use to build
// DOM structure

'use strict';

var mano = require('../../');

module.exports = mano.domjs = require('eregistrations/view/domjs-ext')(require('domjs')(document));
