'use strict';

var mapKeys = require('es5-ext/object/map-keys');

module.exports = function (data) {
	return mapKeys(data, function (name) {
		var i = name.lastIndexOf('/');
		if (i === -1) return name;
		return name.slice(i + 1);
	});
};
