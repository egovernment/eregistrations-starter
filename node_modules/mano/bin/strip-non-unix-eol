#!/usr/bin/env node

'use strict';

Error.stackTraceLimit = Infinity;

// Prevent EMFILE errors
require('fs2/descriptors-handler')();

require('../scripts/strip-non-unix-eol')(process.cwd(),
	{ log: process.stdout.write.bind(process.stdout) }).done();
