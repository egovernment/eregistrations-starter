'use strict';

var remoteSubmit = require('./remote-submit');

module.exports = function () {
	document.body.classList.add('waiting');
	return remoteSubmit.apply(this, arguments).catch(function (e) {
		document.body.classList.remove('waiting');
		throw e;
	});
};
