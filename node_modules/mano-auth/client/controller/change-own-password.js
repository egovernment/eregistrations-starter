'use strict';

var clientHash = require('../../utils/client-hash');

module.exports = function (data, env) {
	data.password = clientHash(env.user.email, data.password);
	data['password-new'] = clientHash(env.user.email, data['password-new']);
	return '/change-own-password-x/';
};
