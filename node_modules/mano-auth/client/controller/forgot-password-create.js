'use strict';

var clientHash = require('../../utils/client-hash');

module.exports = function (data, env) {
	delete data['password-repeat'];
	data.password = clientHash(data.email, data.password);
	return '/forgot-password-create-x/';
};
