'use strict';

var clientController = require('../../client/controller/forgot-password-create')
  , controller       = require('./forgot-password-create-x').controller;

module.exports = function (data, env) {
	clientController(data);
	return controller(data, env);
};
