'use strict';

var clientHash = require('../../utils/client-hash')
  , controller = require('./register-x').controller;

module.exports = function (data, env) {
	data['User#/password'] =
		clientHash(data['User#/email'], data['User#/password']);
	return controller(data, env);
};
