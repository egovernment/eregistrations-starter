'use strict';

var clientController = require('../../client/controller/change-password')
  , controller       = require('./change-password-x').controller;

module.exports = function (data, env) {
	clientController(data);
	return controller(data, env);
};
