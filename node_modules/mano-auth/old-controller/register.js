'use strict';

var validate = require('mano/lib/utils/dbjs-form-validate')
  , Password = require('mano').db.Password;

module.exports = function (data) {
	validate(data, { assure: ['User#/email'], partial: true });
	Password.validate(data['User#/password']);
};
