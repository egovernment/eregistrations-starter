#!/usr/bin/env node

// Generates apps conf
// This module was initially generated by mano/server/setup/generate-apps-conf-script

'use strict';

Error.stackTraceLimit = Infinity;

var resolve = require('path').resolve;

require('mano/scripts/generate-apps-conf')(resolve(__dirname, '..'),
	require('../server/apps/list')).done();
