#!/usr/bin/env node

// Generates apps controllers conf
// This module was initially generated by mano/server/setup/generate-apps-controllers-script

'use strict';

Error.stackTraceLimit = Infinity;

var resolve = require('path').resolve;

require('mano/scripts/generate-apps-controllers')(resolve(__dirname, '..'),
	require('../server/apps/list')).done();
