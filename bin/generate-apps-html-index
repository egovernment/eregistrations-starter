#!/usr/bin/env node

// Generates html indexes for all apps
// This module was initially generated by mano/scripts/generate-apps-html-index-script

'use strict';

Error.stackTraceLimit = Infinity;

var resolve = require('path').resolve;

require('mano/scripts/generate-apps-html-index')(resolve(__dirname, '..'),
	require('../server/apps/list')).done();
