#!/usr/bin/env node

'use strict';

Error.stackTraceLimit = Infinity;

if (!process.env.DEBUG) {
	process.env.DEBUG = 'application-number,auth,clean-demo-users,client,client-bundle,db,' +
		'db-data-sync,health,html-renderer,is-submitted-lock-trigger,load,start-service,zip-archiver';

}

require('../server')();
